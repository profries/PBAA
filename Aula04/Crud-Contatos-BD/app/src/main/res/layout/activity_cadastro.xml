<?xml version="1.0" encoding="utf-8"?>
<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:stretchColumns="1"
    tools:context=".CadastroActivity">
    <TableRow>
        <TextView android:text="Nome: " />
        <EditText android:id="@+id/cadastro_editNome"
            android:layout_width="match_parent"
            android:hint="Nome do Contato"/>
    </TableRow>
    <TableRow>
        <TextView android:text="Telefone: " />
        <EditText android:id="@+id/cadastro_editTelefone"
            android:layout_width="match_parent"
            android:hint="Nº de Telefone"
            android:inputType="phone" />
    </TableRow>
    <LinearLayout android:orientation="horizontal" android:weightSum="1"
        android:baselineAligned="false"
        android:longClickable="false"
        android:gravity="center_horizontal">


        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Cancelar"
            android:id="@+id/cadastro_botaoCancelar"
            android:onClick="cancelarCadastro" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Cadastrar"
            android:id="@+id/cadastro_botaoCadastrar"
            android:onClick="cadastrarContato" />
    </LinearLayout>
</TableLayout>
